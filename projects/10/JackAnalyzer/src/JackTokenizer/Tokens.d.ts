export default class Tokens {
    private baseTokens;
    constructor(baseTokens: string[]);
    parse(): string[];
    private parseOneBaseToken;
    private recursiveParse;
    private buildStringToken;
}
//# sourceMappingURL=Tokens.d.ts.map