export default class Tokens {
    private baseTokens: string[];

    constructor(baseTokens: string[]) {
        this.baseTokens = baseTokens;
    }

    public parse(): string[] {
        //const chunks: string[][] = this.baseTokens.map((bt: string): string[] => {})
    }
}
